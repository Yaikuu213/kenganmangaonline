name: Build and Deploy

on:
  push:
    branches: [ main ] # On surveille la branche main
  workflow_dispatch:      # Permet de lancer le robot à la main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Main
        uses: actions/checkout@v4
        with:
          ref: main 

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'adopt'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Build Extension
        run: |
          # MODIF : On utilise 'gradle' directement au lieu de './gradlew'
          gradle :src:en:kenganmanga:assembleRelease

      - name: Prepare Distribution Folder
        run: |
          mkdir -p ../dist/apk
          # On cherche l'APK généré et on le déplace pour le magasin
          find . -name "*.apk" -exec cp {} ../dist/apk/eu.kanade.tachiyomi.extension.en.kenganmanga-v1.apk \;
          
          # On génère le catalogue JSON pour Tachimanga
          cat << 'EOF' > ../dist/index.min.json
          [
            {
              "name": "Kengan Manga Online",
              "pkg": "eu.kanade.tachiyomi.extension.en.kenganmanga",
              "apk": "apk/eu.kanade.tachiyomi.extension.en.kenganmanga-v1.apk",
              "lang": "en",
              "code": 5,
              "version": "1.2.0",
              "sources": [
                {
                  "name": "Kengan Manga Online",
                  "id": "7038148785664273523",
                  "lang": "en",
                  "baseUrl": "https://w15.kengan-manga.com"
                }
              ]
            }
          ]
          EOF
          cp ../dist/index.min.json ../dist/index.json

      - name: Deploy to Branch Repo
        uses: s0/git-publish-subdir-action@develop
        env:
          REPO: self
          BRANCH: repo # Branche où l'APK sera stocké
          FOLDER: ../dist
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          MESSAGE: "🚀 Mise à jour du magasin : v1.2.0"
